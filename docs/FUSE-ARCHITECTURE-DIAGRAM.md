# USB Enforcer with Content Scanning - System Architecture

## Complete System Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           USER SPACE (Desktop Environment)                   │
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  File Manager (Nautilus/Dolphin/Thunar)                               │ │
│  │  - User drags files to USB                                            │ │
│  │  - Sees FUSE mount point: /media/user/usb-device/                    │ │
│  └───────────────────────────┬───────────────────────────────────────────┘ │
│                              │ write("/media/user/usb-device/file.txt")    │
│                              ▼                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  FUSE Overlay Filesystem (ContentScanningFuse)                        │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐ │ │
│  │  │  write() intercepted:                                            │ │ │
│  │  │  1. Buffer data in memory                                        │ │ │
│  │  │  2. Track progress (ScanProgress)                                │ │ │
│  │  │  3. On close(): Trigger scan                                     │ │ │
│  │  └─────────────────────────────────────────────────────────────────┘ │ │
│  │                              ▼                                          │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐ │ │
│  │  │  Content Scanner (ContentScanner)                                │ │ │
│  │  │  ┌───────────────────────────────────────────────────────────┐ │ │ │
│  │  │  │ Pattern Library (27 patterns):                            │ │ │ │
│  │  │  │ - SSN, credit cards, bank accounts                        │ │ │ │
│  │  │  │ - API keys, passwords, tokens                             │ │ │ │
│  │  │  │ - Passport numbers, driver licenses                       │ │ │ │
│  │  │  │ - Medical records, insurance IDs                          │ │ │ │
│  │  │  └───────────────────────────────────────────────────────────┘ │ │ │
│  │  │  ┌───────────────────────────────────────────────────────────┐ │ │ │
│  │  │  │ Archive Scanner:                                          │ │ │ │
│  │  │  │ - Extract ZIP, TAR, 7Z, RAR                              │ │ │ │
│  │  │  │ - Recursive scanning (max depth 5)                       │ │ │ │
│  │  │  └───────────────────────────────────────────────────────────┘ │ │ │
│  │  │  ┌───────────────────────────────────────────────────────────┐ │ │ │
│  │  │  │ Document Scanner:                                         │ │ │ │
│  │  │  │ - Extract text from PDF, DOCX, XLSX, PPTX, ODF          │ │ │ │
│  │  │  └───────────────────────────────────────────────────────────┘ │ │ │
│  │  │  ┌───────────────────────────────────────────────────────────┐ │ │ │
│  │  │  │ N-gram Analyzer:                                          │ │ │ │
│  │  │  │ - Entropy analysis (Shannon)                              │ │ │ │
│  │  │  │ - Suspicious pattern detection                            │ │ │ │
│  │  │  └───────────────────────────────────────────────────────────┘ │ │ │
│  │  │  ┌───────────────────────────────────────────────────────────┐ │ │ │
│  │  │  │ Cache (LRU):                                              │ │ │ │
│  │  │  │ - SHA-256 content hashing                                 │ │ │ │
│  │  │  │ - 100 MB cache (configurable)                             │ │ │ │
│  │  │  │ - 60-70% hit rate typical                                 │ │ │ │
│  │  │  └───────────────────────────────────────────────────────────┘ │ │ │
│  │  └─────────────────────────────────────────────────────────────────┘ │ │
│  │                              ▼                                          │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐ │ │
│  │  │  Scan Result:                                                    │ │ │
│  │  │  ✅ ALLOWED → write to .real/ → Physical USB                    │ │ │
│  │  │  ⛔ BLOCKED → reject write → EPERM error to application         │ │ │
│  │  └─────────────────────────────────────────────────────────────────┘ │ │
│  └─────────┬─────────────────────────────────────────────────────────────┘ │
│            │ emit_scan_progress()                                          │
│            ▼                                                                │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  DBus System Bus (org.seravault.UsbEnforcer)                         │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐ │ │
│  │  │  Signals:                                                        │ │ │
│  │  │  - ScanProgress(filepath, progress, status, total, scanned)     │ │ │
│  │  │  - Event(fields)                                                 │ │ │
│  │  │                                                                  │ │ │
│  │  │  Methods:                                                        │ │ │
│  │  │  - GetScannerStatistics() → Dict[str, str]                      │ │ │
│  │  │  - RequestUnlock(devnode, mapper, passphrase)                   │ │ │
│  │  │  - RequestEncrypt(...)                                           │ │ │
│  │  └─────────────────────────────────────────────────────────────────┘ │ │
│  └─────────┬─────────────────────────────────────────────────────────────┘ │
│            │ ScanProgress signal                                           │
│            ▼                                                                │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  Notification Service (usb-enforcer-notifications)                    │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐ │ │
│  │  │  GTK 4 Application                                               │ │ │
│  │  │  ┌───────────────────────────────────────────────────────────┐ │ │ │
│  │  │  │  ScanNotificationWindow:                                   │ │ │ │
│  │  │  │  ┌──────────────────────────────────────────────────────┐ │ │ │ │
│  │  │  │  │ 🔍 Scanning File for Sensitive Data               │ │ │ │ │
│  │  │  │  │ ─────────────────────────────────────────────────  │ │ │ │ │
│  │  │  │  │ File: confidential-report.pdf                     │ │ │ │ │
│  │  │  │  │ [████████████████░░░░░░░░░░] 65%                  │ │ │ │ │
│  │  │  │  │ Status: Scanning for sensitive data...            │ │ │ │ │
│  │  │  │  │                                                    │ │ │ │ │
│  │  │  │  │ Details ▼                                          │ │ │ │ │
│  │  │  │  │   Size: 2.3 MB / 3.5 MB                           │ │ │ │ │
│  │  │  │  │   Speed: 5.2 MB/s                                 │ │ │ │ │
│  │  │  │  │   Patterns checked: 27                            │ │ │ │ │
│  │  │  │  └──────────────────────────────────────────────────────┘ │ │ │ │
│  │  │  └───────────────────────────────────────────────────────────┘ │ │ │
│  │  │  ┌───────────────────────────────────────────────────────────┐ │ │ │
│  │  │  │  Desktop Notification (for blocked files):              │ │ │ │
│  │  │  │  ┌─────────────────────────────────────────────────────┐ │ │ │ │
│  │  │  │  │ ⛔ USB File Blocked                                 │ │ │ │ │
│  │  │  │  │ File 'customer-data.xlsx' was blocked from USB.    │ │ │ │ │
│  │  │  │  │ Reason: Contains Social Security Numbers           │ │ │ │ │
│  │  │  │  └─────────────────────────────────────────────────────┘ │ │ │ │
│  │  │  └───────────────────────────────────────────────────────────┘ │ │ │
│  │  └─────────────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                           SYSTEM SPACE (Root Daemon)                         │
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  USB Enforcer Daemon (usb-enforcerd)                                  │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐ │ │
│  │  │  Initialization:                                                 │ │ │
│  │  │  1. Load config (/etc/usb-enforcer/config.toml)                 │ │ │
│  │  │  2. Initialize content scanner (if enabled)                     │ │ │
│  │  │  3. Create FUSE manager                                          │ │ │
│  │  │  4. Start DBus service                                           │ │ │
│  │  │  5. Start udev monitor                                           │ │ │
│  │  └─────────────────────────────────────────────────────────────────┘ │ │
│  │                              ▼                                          │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐ │ │
│  │  │  Device Event (udev):                                            │ │ │
│  │  │  1. USB device inserted                                          │ │ │
│  │  │  2. Classify device (plain/encrypted)                            │ │ │
│  │  │  3. Enforce policy:                                              │ │ │
│  │  │     - Plain: Block read-write, allow read-only                  │ │ │
│  │  │     - Encrypted: Prompt for unlock                               │ │ │
│  │  └─────────────────────────────────────────────────────────────────┘ │ │
│  │                              ▼                                          │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐ │ │
│  │  │  On Unlock (request_unlock):                                     │ │ │
│  │  │  1. Unlock LUKS volume (cryptsetup)                             │ │ │
│  │  │  2. Wait for automount (udisks2)                                 │ │ │
│  │  │  3. Get mount point (/media/user/...)                           │ │ │
│  │  │  4. Setup FUSE overlay:                                          │ │ │
│  │  │     a. Remount device at .real/                                  │ │ │
│  │  │     b. Mount FUSE at original mount point                        │ │ │
│  │  │     c. Register progress callback                                │ │ │
│  │  │  5. User now sees FUSE overlay                                   │ │ │
│  │  └─────────────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                           KERNEL SPACE                                       │
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  Udev Rules (/etc/udev/rules.d/49-usb-enforcer.rules)                │ │
│  │  - Detect USB storage devices                                         │ │
│  │  - Apply blockdev --setro for plain devices                           │ │
│  │  - Set UDISKS_AUTO=1 for encrypted devices                            │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                              ▼                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  Device Mapper (dm-crypt)                                             │ │
│  │  - LUKS2 encrypted volumes                                            │ │
│  │  - AES-XTS-512 encryption                                             │ │
│  │  - Argon2id KDF                                                       │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                              ▼                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  Block Device Layer                                                   │ │
│  │  - /dev/sda1 (physical partition)                                     │ │
│  │  - /dev/mapper/usbenc-sda1 (decrypted)                                │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                              ▼                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  Filesystem (exFAT/ext4/NTFS)                                         │ │
│  │  - Mounted at /media/user/usb-device/.real/                           │ │
│  │  - Hidden from user                                                   │ │
│  │  - Receives writes only after FUSE approval                           │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                              ▼                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  USB Mass Storage Driver                                              │ │
│  │  - USB 2.0/3.0 protocol                                               │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                              ▼                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  Physical USB Device                                                  │ │
│  │  - Flash storage                                                      │ │
│  │  - Contains only allowed data                                         │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## Data Flow: Copy File to USB

```
Step 1: User Action
├─ User: cp file.txt /media/user/usb-device/
└─ Application issues write() syscall

Step 2: FUSE Interception
├─ FUSE kernel module redirects to userspace
├─ ContentScanningFuse.write() called
├─ Data buffered in memory (self._write_buffers[path])
└─ Return success to application (buffer accepted)

Step 3: File Close
├─ Application: close(fd)
├─ FUSE kernel module calls release()
├─ ContentScanningFuse.release() triggered
└─ Begin content scanning

Step 4: Content Scanning
├─ Get complete buffered content
├─ Determine file type (text/archive/document)
├─ Archive? → Extract and scan recursively
├─ Document? → Extract text (PDF/DOCX/etc)
├─ Text? → Pattern matching
│   ├─ Check 27 built-in patterns
│   ├─ Run validators (SSN format, Luhn algorithm, etc)
│   ├─ N-gram analysis (entropy calculation)
│   └─ Check cache (SHA-256 hash lookup)
└─ Generate ScanResult (BLOCKED/ALLOWED)

Step 5: Progress Updates
├─ During scan: emit progress via callback
├─ Daemon: _notify_progress() called
├─ DBus: emit ScanProgress signal
├─ Notification: ScanProgressListener receives signal
├─ GTK: Update progress bar and status
└─ User sees: "Scanning... 45%"

Step 6: Decision
├─ ScanResult.blocked == True?
│   ├─ YES: BLOCKED
│   │   ├─ Return -EPERM to application
│   │   ├─ Delete write buffer
│   │   ├─ Log: "BLOCKED - patterns=['ssn', 'credit_card']"
│   │   ├─ DBus: emit ScanProgress(..., status='blocked')
│   │   ├─ Notification: Show desktop notification
│   │   └─ User sees: "⛔ USB File Blocked - Contains SSN"
│   │
│   └─ NO: ALLOWED
│       ├─ Write buffer to .real/ mount
│       ├─ Data reaches physical USB
│       ├─ Log: "ALLOWED - no sensitive data"
│       ├─ DBus: emit ScanProgress(..., status='allowed')
│       ├─ Notification: Window auto-closes (3s)
│       └─ File successfully written

Step 7: Statistics Update
├─ Increment files_scanned counter
├─ Increment files_blocked OR files_allowed
├─ Add to total_bytes_scanned
├─ Update cache_hit_rate
└─ Available via GetScannerStatistics()
```

## Configuration Flow

```
/etc/usb-enforcer/config.toml
    ↓
src/usb_enforcer/config.py
    ├─ Config.load()
    ├─ Parse [content_scanning] section
    ├─ ContentScanningConfig.from_dict()
    │   ├─ enabled: bool
    │   ├─ action: "block"/"warn"/"log_only"
    │   ├─ enabled_categories: List[str]
    │   ├─ custom_patterns: List[Pattern]
    │   └─ Performance settings
    └─ Return Config object

src/usb_enforcer/daemon.py
    ├─ Daemon.__init__()
    ├─ self.config = Config.load()
    ├─ self._init_content_verification()
    │   ├─ Check CONTENT_SCANNER_AVAILABLE
    │   ├─ Check config.content_scanning.enabled
    │   ├─ ContentScanner(config.content_scanning)
    │   │   ├─ Load pattern library
    │   │   ├─ Initialize cache
    │   │   └─ Setup archive/document scanners
    │   └─ FuseManager(scanner=self.content_verification)
    └─ Ready for device events

Device Unlock
    ├─ request_unlock()
    ├─ crypto_engine.unlock_luks()
    ├─ Wait for automount
    ├─ _setup_fuse_overlay(mount_point)
    │   ├─ Remount device at .real/
    │   ├─ fuse_manager.mount(mount_point)
    │   │   ├─ Create ContentScanningFuse(mount_point)
    │   │   ├─ Register progress_callback
    │   │   └─ FUSE.mount() in background thread
    │   └─ User sees FUSE overlay
    └─ All writes now intercepted and scanned
```

## Performance Optimization Points

```
1. Cache Layer (SHA-256 hashing)
   ├─ Hit: < 0.1ms (instant)
   ├─ Miss: Full scan required
   └─ Hit rate: 60-70% typical

2. 3-Tier Scanning Strategy
   ├─ Small (<1MB): Full scan
   ├─ Medium (1-10MB): Chunked scan
   └─ Large (>10MB): Sampled scan

3. Concurrent Scanning
   ├─ max_concurrent_scans: 2 (default)
   ├─ ThreadPoolExecutor for parallelism
   └─ Per-file progress tracking

4. Archive Depth Limiting
   ├─ max_archive_depth: 5
   ├─ Prevents zip bombs
   └─ Limits extraction time

5. File Size Limits
   ├─ max_file_size_mb: 100
   ├─ Skip huge files
   └─ Prevent memory exhaustion

6. Scan Timeout
   ├─ scan_timeout_seconds: 30
   ├─ Prevent hanging
   └─ Return error if exceeded
```

## Security Boundaries

```
┌─────────────────────────────────────────────────────────┐
│                   TRUSTED ZONE                          │
│  ┌───────────────────────────────────────────────────┐ │
│  │  Root Daemon (usb-enforcerd)                      │ │
│  │  - Runs as UID 0                                  │ │
│  │  - Full device access                             │ │
│  │  - FUSE mount authority                           │ │
│  │  - Udev event processing                          │ │
│  └───────────────────────────────────────────────────┘ │
│                       ▲ DBus                            │
│  ┌───────────────────────────────────────────────────┐ │
│  │  DBus Policy (/etc/dbus-1/system.d/)              │ │
│  │  - Allow root to own service                      │ │
│  │  - Allow users in plugdev to call methods         │ │
│  │  - Allow all to receive signals                   │ │
│  └───────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
                       ▲ DBus System Bus
┌─────────────────────────────────────────────────────────┐
│                   USER ZONE                             │
│  ┌───────────────────────────────────────────────────┐ │
│  │  Notification Service (usb-enforcer-notifications)│ │
│  │  - Runs as logged-in user                         │ │
│  │  - Receives scan progress signals                 │ │
│  │  - Displays GTK windows                           │ │
│  │  - No device access                               │ │
│  └───────────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────────┐ │
│  │  User Applications                                │ │
│  │  - File manager, terminal, etc                    │ │
│  │  - Writes to FUSE mount point                     │ │
│  │  - No direct device access                        │ │
│  └───────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘

Security Properties:
✅ User cannot bypass FUSE (udev rules enforce read-only on raw device)
✅ User cannot unmount FUSE (requires root)
✅ User cannot kill daemon (systemd restarts it)
✅ User cannot modify cache (runs as root, uses crypto hashing)
✅ User cannot see pattern matches (only categories logged)
✅ DBus enforces least privilege (polkit integration)
```

This architecture provides **defense in depth** with multiple layers protecting against data exfiltration.
